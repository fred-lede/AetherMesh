# AetherMesh - 任務追蹤

<!-- auto-generated by opencode -->

## 2026-04-18

### Completed

- [x] 修復 `providers/ollama_adapter.py` 語法錯誤 (重複程式碼片段)
- [x] 清除 Python cache 解決載入問題
- [x] 恢復 Dashboard 科技感樣式 (index.html + style.css)
- [x] 新增 `dashboard/static/style.css` 及 static routes
- [x] **HTTP Session 池** - 新建 `providers/http_client.py`，所有 providers 使用共享連線池
- [x] Router & Dashboard 使用 session
- [x] 新增 `.env.example` HTTP 池設定
- [x] **Rate Limiting** - 新建 `router/rate_limiter.py`，Token bucket 實作
- [x] 套用到 Router API
- [x] **多 Task Worker** - 支援多實例平行處理異步任務
- [x] 新增 `scripts/start_task_worker.bat`
- [x] task_worker 使用 HTTP session
- [x] **Dashboard Health API** - 新增 `/api/health` 給監控系統
- [x] **Dashboard 圖表** - 加入 Latency/Request Rate 即時圖表 (Chart.js)
- [x] **GPU 趨勢圖** - 加入 GPU Utilization 趨勢圖 (多線)
- [x] **Model 使用統計** - 加入 Doughnut 圖
- [x] **Worker 負載分布** - 加入 Horizontal Bar 圖
- [x] **延遲分佈** - 加入 P50/P95/P99 Bar 圖
- [x] **智能負載平衡** - 權重分散 + model 親和性 + 混合策略
- [x] **GPU 故障檢測** - 強化 heartbeat + dead 標記
- [x] **任務重排** - 失敗時重新排隊
- [x] **多層 Fallback Chain** - model → fallback → queue → error
- [x] **Circuit Breaker** - 防止連鎖故障 (CLOSED → OPEN → HALF_OPEN)
- [x] **Structured Logging** - JSON 格式日誌 + correlation ID
- [x] **專業圖表** - Tokens/sec, TTFT, Memory, Power, Temperature
- [x] **NVIDIA NIM Provider** - 新增 `providers/nvidia_nim_adapter.py`，OpenAI 相容 API
- [x] **Ollama Cloud Provider** - 新增 `providers/ollama_cloud_adapter.py`，支援 ollama.com 雲端模型
- [x] **Anthropic Router** - 新建 `router/anthropic_router.py`，完整 Anthropic Messages API 相容
- [x] **Cloud Provider Health Monitoring** - Dashboard 新增 `/api/providers/health` 與 UI 狀態顯示
- [x] **Full Anthropic Features** - Extended Thinking, Prompt Caching, Tools, Vision, Documents
- [x] **Anthropic Router Fixes** - 修正 null usage 處理、移除 `extra_body`/`anthropic_beta` 避免上游驗證錯誤
- [x] **Request Metrics Collector** - 新建 `metrics/request_metrics.py`，追蹤每個請求的 tokens/延遲/provider/錯誤
- [x] **Model Routing Engine** - 新建 `router/routing_engine.py`，基於能力的 provider 路由選擇
- [x] **Routing Rules Config** - 新增 `config/routing_rules.yaml`，可配置路由規則與 model overrides
- [x] **Dashboard Request Metrics UI** - 請求指標摘要卡片 + 每 provider 指標表格
- [x] **Dashboard Routing Controls UI** - Provider 啟用/禁用開關 + Model Override 新增/刪除控制
- [x] **Dashboard API Endpoints** - `/api/metrics/*` 和 `/api/routing/*` 端點
- [x] **Anthropic Router Integration** - 整合 metrics 記錄與 routing engine 到 `/v1/messages` 端點
- [x] **Chinese Encoding Fix** - SSE 流式輸出改用 `ensure_ascii=True` 防止反向代理導致中文亂碼
- [x] **Claude Code 中文亂碼修復** - Anthropic 非串流 JSON/錯誤回覆改用 ASCII-safe JSON，OpenAI/NVIDIA NIM/Gemini 上游回覆強制 UTF-8 解碼
- [x] **Dashboard 認證** - 新增可選登入頁 + session cookie，保留 Basic Auth 與 health endpoints 免認證
- [x] **Dashboard Routing 操作回饋** - Provider enable/disable 與 model override set/remove 顯示成功/失敗訊息，操作時按鈕進入 busy 狀態
- [x] **Provider 故障診斷** - Dashboard provider cards 顯示最後成功、最後錯誤與連續失敗次數，Anthropic router 暴露 provider diagnostics metrics
- [x] **Routing 狀態持久化** - Dashboard provider enable/disable 與 model overrides 寫入 `config/routing_state.yaml`，重啟後保留
- [x] **Routing 操作審計紀錄** - Provider toggle 與 model override 變更寫入 `config/routing_audit.jsonl`，Dashboard 顯示最近活動
- [x] **Local Only Mode** - Dashboard 一鍵停用雲端 provider，只保留本機 Ollama，並可一鍵恢復全部 provider
- [x] **Local Only 本機模型 fallback** - Local Only 或 `model -> ollama` override 遇到未知雲端模型名時，自動改派到 registry 中可用的本機 Ollama 模型
- [x] **Dashboard Model Alias 對照表** - Routing Controls 顯示 Gateway model、AIIH alias、實際模型、provider、worker 與 capabilities
- [x] **Cloud Alias 顯示語意** - 雲端 alias 在 Dashboard worker 欄位顯示 cloud adapter/base URL，不再顯示為 not configured
- [x] **文件同步至目前版本** - README 與 env profile 範本更新 Dashboard alias map、cloud adapter、fallback、node_hosts 與 Claude gateway 設定說明
- [x] **Tool-call 偵錯日誌降噪** - Ollama 串流 tool_calls 診斷改為 DEBUG level，並略過空 summary 避免正常串流刷屏
- [x] **NIM 404 fallback 語意** - NVIDIA NIM 404 歸類為 model_not_found，Anthropic/OpenAI 路由可 fallback 到本機 Ollama，Dashboard 顯示 model unavailable
- [x] **NIM 502 fallback 語意** - NVIDIA NIM 502 Bad Gateway 歸類為 provider_overloaded，Anthropic/OpenAI 路由可 fallback 到本機 Ollama，Dashboard 顯示 upstream gateway
- [x] **NIM timeout 調整** - 預設 `NVIDIA_NIM_TIMEOUT` 從 12 秒調整為 120 秒，避免 NIM cold start 被過早 fallback
- [x] **z-ai NIM heuristic 對齊** - OpenAI router fallback heuristic 將 `z-ai/` 歸類到 NVIDIA NIM，與 Anthropic router 保持一致
- [x] **Ollama Cloud content 正規化** - Ollama Cloud adapter 將 OpenAI/Anthropic content blocks 攤平成 Ollama `/api/chat` 可接受的字串與 images
- [x] **Content block 轉譯 helper** - 新增 `router/content_blocks.py`，集中轉譯 Anthropic/OpenAI/Ollama content parts；支援 text/plain document 解碼、PDF/文件提示、nested tool_result image、input_audio 與 Ollama audio hint
- [x] **Capability gating / fallback** - 新增 `router/capabilities.py`，Anthropic/OpenAI 入口統一推導 `chat/tools/thinking/vision/audio` 需求；routing engine 不再因 exact model match 選到缺能力的模型，會改找符合能力的 local Ollama fallback
- [x] **Provider cooldown / probe 恢復** - Cloud provider timeout/overload/rate limit/model_not_found 後暫時跳過，Dashboard 顯示 cooldown，probe 或成功請求會清除 cooldown
- [x] **Tool-use 外露收斂** - Anthropic 非串流回覆同時含 raw tool-use 文字與 tool_calls 時會合併為 tool_use block 並隱藏 raw JSON
- [x] **Probe 狀態顯示修正** - Provider probe/health 通過且沒有連續失敗時，Dashboard 不再被舊 timeout 訊息覆蓋為 timeout
- [x] **串流 tool-use 前導文字緩衝** - Anthropic SSE 對 `Claude responded:` / WebSearch raw tool-use 前導文字先緩衝，避免 tool call 前半段被當一般文字輸出
- [x] **Tool-call normalizer / gate** - 新增 `router/tool_call_normalizer.py`，統一解析 OpenAI `tool_calls`、Anthropic/raw `tool_use`、`<tool_use>` 與 Python dict 文字；未在 request 宣告的工具會被靜默隔離並寫入 log，不再外露 raw JSON 或觸發工具重試迴圈
- [x] **OMLX-style 多模型工具標記解析** - Tool-call normalizer 追加 Qwen/Llama `<function=...>`、GLM `<arg_key>/<arg_value>`、namespaced `<*:tool_call>`、`[Tool call: ...]`、Gemma `call:name{...}` 等格式；同時剝離 `<think>` 內工具標記，避免 reasoning 誤觸發工具
- [x] **Server tool 前置 policy** - 新增 `router/server_tool_policy.py`，送 OpenAI-compatible provider 前先辨識 `web_search` / `web_fetch` Anthropic server tools；目前未啟用本地 web tools 時直接拒絕，不再把 server tool 偽裝成 function 丟給 NIM/Ollama 造成搜尋迴圈
- [x] **本地 Web server tools SSE handler** - 新增 `router/web_server_tools.py` 與 `AIIH_WEB_SERVER_TOOLS_ENABLED`，當 request 以 `tool_choice` 強制 `web_search` / `web_fetch` 時由 AIIH 直接回 Anthropic `server_tool_use`、tool result 與文字摘要
- [x] **Anthropic SSE builder** - 新增 `router/anthropic_sse_builder.py`，集中管理 text/thinking/tool_use content blocks、stop reason mapping、tool arguments pre-buffer，Anthropic router 串流輸出不再手刻分散的 block state
- [x] **Server tool mode 切換** - 新增 `AIIH_SERVER_TOOL_MODE=reject|local|passthrough`，可選擇由 AIIH 拒絕、AIIH 本地處理，或保留 Claude Code/runtime client 的內建 WebSearch 流程

### In Progress

- [ ] (none)

### Pending

- [ ] (none)

---

## 歷史任務

### 2026-04-17

- [x] v4 初始發布 - 多 GPU + 多節點叢集
- [x] Router API 實作 (OpenAI 相容)
- [x] Control Plane 實作
- [x] Dashboard 實作
- [x] Prometheus metrics 實作
